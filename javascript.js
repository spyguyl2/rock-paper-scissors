function getComputerChoice () {
    let x = Math.floor(Math.random() * 3)
    switch (x) {
        case 0:
            
            break;
        case 1:

            break;
        case 2:

            break;
        default:
            break;
    }
}